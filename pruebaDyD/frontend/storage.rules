rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir todo para desarrollo (luego puedes restringir)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}